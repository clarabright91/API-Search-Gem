<%= content_for :html_title, @category.name %>
<div class="container">
  <div class="row">
    <div class="col-md-8">
      <div class="blog-post-box">
        <!-- Category name -->
        <h2><%= @category.name %></h2>

        <!-- Recent posts -->
        <h4>Recent posts</h4>

        <ul class="author-posts">
          <% @category.recent_posts.each do |post| %>
            <li><%= render :partial => 'buttercms/posts/post', :locals => {:post => post} %></li>
          <% end %>
        </ul>

        </div>
        </div>
      <%= render 'partials/butter_cms_side_form'%>
  </div>
</div>