<!-- header start -->
<header>
  <div class="container">
   <nav class="navbar navbar-default navbar-tall navbar-full">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#global-nav" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!-- <a class="navbar-brand" href=""><%#= image_tag("logo.png", :alt => "PureLOAN") %></a> -->
      <%= link_to image_tag("logo.png", :alt => "PureLOAN"),root_path, class: "navbar-brand" %>
    </div>
    <div class="navbar-collapse collapse" id="global-nav" aria-expanded="false">
     <div class="navbar-right">
      <ul class="nav navbar-nav">
        <li>
          <%=link_to("Mortgage Rates", mortgage_path)%>
        </li>
        <li>
           <%= link_to 'Refinance Rates', refinance_path%> 
        </li>
        <li>
          <%= link_to 'Calculator', calculator_path%>
        </li>
        <% if user_signed_in? %>
          <!-- <li>
            <%= link_to("Profile", edit_user_registration_path)%>
          </li>
          <li>
            <%= link_to("Price Alert", '#') %>
          </li> -->
          <li>
            <%= link_to("Settings", edit_user_registration_path)%>
          </li>
          <li>
            <%= link_to("Logout", destroy_user_session_path) %>
          </li>
        <% else %>
          <li>
            <a href="" data-toggle="modal" data-target="#login-popup"  class="login-btn">LOGIN</a>
          </li>
        <% end %>
      </ul>
     </div>
    </div>
   </nav>
  </div>
</header>
<!-- header end-->

<script type="text/javascript">
  $(function() {
    $('.login-btn').click(function(){
      $('.login-mdl-header').show();
      $('.price-alert-header').hide();
    });
    $('.price-alert-btn').click(function(){
      $('.login-mdl-header').hide();
      $('.price-alert-header').show();
    });
    $('.signup-link-clr').click(function(){
      $('.login-mdl-header').show();
      $('.price-alert-header').hide();
    });

    

    var pgurl = window.location.href.substr(window.location.href
      .lastIndexOf("/")+1);
        $(".navbar-nav a").each(function(){
          if($(this).attr("href").split("/")[1]  == pgurl || $(this).attr("href").split("/")[2]  == pgurl)
            $(this).closest("li").addClass("active");
          else
            $(this).closest("li").removeClass("active");   
        })
    });

</script>